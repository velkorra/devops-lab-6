apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: work-app-sm
  namespace: monitoring  
  labels:
    release: monitoring 
spec:
  namespaceSelector:
    matchNames:
      - lab6            
  selector:
    matchLabels:
      app: work-app
  endpoints:
  - port: http
    path: /q/metrics
    interval: 1s